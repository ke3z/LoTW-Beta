<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Electronic QSL Testing</title>
</head>

<body>

    <style>
        body {
            font-family: sans-serif;
            font-size: 14px;
			padding: 0.5rem 1rem;
        }

        pre {
            font-size: 80%;
            white-space: pre-wrap;
            overflow-x: auto;
        }

        .table2 {
            display: grid;
            grid-template-columns: repeat(2, auto);
            gap: 0.2rem 0.4rem;
            width: min-content;
            margin-left: 2rem;
        }

        .table2>div {
            white-space: nowrap;
        }

        .table2>div:nth-child(odd) {
            font-weight: bold;
            text-align: right;
        }

        img.qslcard {
            border: solid gray 2px;
            width: 800px;
        }

        .top2 {
            display: flex;
            gap: 1.75rem;
        }

        .top2>div {
            border: solid lightgray 1px;
            padding: .5rem .8rem;
        }

        .top2 h2 {
            margin-top: 0;
        }

        #imgs {
			position: relative;
            margin-top: 2rem;
			height: 600px;
        }
		.warnings {
			color: red;
			font-weight: bold;
			margin: 0.25rem 0;
		}
		#imgs a {
			margin-right: 0.5rem;
		}
		.links {
			display: flex;
			flex-direction: column;
			gap: 0.25rem;
		}
		.shield {
			display: none;
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background-color: white;
			opacity: 75%;
		}
		label {
			margin-right: 1rem;
		}
    </style>

    <h1>Electronic QSL Testing</h1>
{{# err }}
	<div class="warnings">{{ err }}</div>
{{/ err }}

